# mcp-files

Safe file operations in `/data/agent` sandbox.

## Endpoints

- `GET /files` - List files
- `GET /files/{filename}` - Read file
- `PUT /files/{filename}` - Write file
- `DELETE /files/{filename}` - Delete file
